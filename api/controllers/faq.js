module.exports = {
    exits: {
        success: {
            viewTemplatePath: 'faq',
        }
    },
    

    fn: async function () {

        // Respond with view.
        return { };
    
    }
}